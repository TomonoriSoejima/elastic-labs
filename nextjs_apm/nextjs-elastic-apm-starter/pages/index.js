export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Next.js + Elastic APM (server-side) ✅</h1>
      <p>Open <code>/api/hello</code> to generate some traffic.</p>
      <p>Visit your Kibana APM app to see transactions named by pages and API routes.</p>
    </main>
  );
}